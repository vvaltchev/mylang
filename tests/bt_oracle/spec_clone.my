# a specialization clone name$sN (a const second argument)
func sc(a, b) {
    var t = a * 2;
    if (b > 3) { t = t + 0; }
    return 100 / (t - b);
}
func drive(n) {
    var s = 0;
    for (var i = 0; i < n; i++) s = s + sc(int(runtime(i)), 4);
    return s;
}
print(drive(runtime(5)));
